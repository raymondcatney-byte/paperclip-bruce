# Render Blueprint for Paperclip
services:
  - type: web
    name: paperclip-bruce
    runtime: node
    region: ohio
    plan: free
    buildCommand: |
      npm install -g pnpm
      NODE_ENV=development pnpm install
      echo "=== Building all workspace packages ==="
      pnpm -r build 2>&1 || echo "Build completed with some errors"
      echo "=== Verifying build output ==="
      ls -la packages/db/dist/ 2>/dev/null || echo "db/dist not found"
      ls -la server/dist/ 2>/dev/null || echo "server/dist not found"
      cd packages/db && npx drizzle-kit generate
    startCommand: |
      export HOST="0.0.0.0"
      export PORT="10000"
      export NODE_ENV="production"
      export PAPERCLIP_MIGRATION_PROMPT="never"
      echo "Starting with HOST=$HOST PORT=$PORT"
      cd packages/db && npx drizzle-kit migrate && cd ../../server && node dist/index.js 2>&1
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 10000
      - key: PAPERCLIP_MIGRATION_PROMPT
        value: never
      - key: DATABASE_URL
        fromDatabase:
          name: paperclip-db
          property: connectionString
    healthCheckPath: /api/health

databases:
  - name: paperclip-db
    databaseName: paperclip
    user: paperclip
    plan: free
    region: ohio
