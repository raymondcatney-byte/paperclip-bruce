# Paperclip Cloud Deployment
services:
  - type: web
    name: paperclip-bruce
    runtime: node
    region: ohio
    plan: free
    buildCommand: |
      npm install -g pnpm
      NODE_ENV=development pnpm install
    startCommand: |
      cd packages/db && npx drizzle-kit migrate && 
      echo "Note: Use auth bootstrap-ceo command to create first admin" && 
      cd ../../server && 
      HOST=0.0.0.0 PORT=10000 NODE_ENV=production pnpm exec tsx src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 10000
      - key: PAPERCLIP_DEPLOYMENT_MODE
        value: authenticated
      - key: PAPERCLIP_DEPLOYMENT_EXPOSURE
        value: public
      - key: PAPERCLIP_PUBLIC_URL
        value: https://paperclip-bruce.onrender.com
      - key: BETTER_AUTH_SECRET
        generateValue: true
      - key: PAPERCLIP_MIGRATION_PROMPT
        value: never
      - key: DATABASE_URL
        fromDatabase:
          name: paperclip-db
          property: connectionString
    healthCheckPath: /api/health

databases:
  - name: paperclip-db
    databaseName: paperclip
    user: paperclip
    plan: free
    region: ohio
